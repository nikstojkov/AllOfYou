<%= simple_form_for [@opportunity, @application] do |f| %>
  <%= f.label "Please add message:" %>
  <%= f.input :content, label: false %>
  <%= f.submit "Apply", class: "btn btn-primary" %>
<% end %>
