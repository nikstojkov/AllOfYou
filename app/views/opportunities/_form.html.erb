<%= simple_form_for(opportunity) do |f| %>
  <%= f.input :title %>
  <%= f.input :location %>
  <%= f.input :description %>
  <%# Adding status for now, as not sure how it will be set yet %>
  <%= f.input :status %>
<% end %>
